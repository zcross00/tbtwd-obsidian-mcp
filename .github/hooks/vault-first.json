{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "echo {\"systemMessage\": \"VAULT-FIRST PROTOCOL ACTIVE: Before any design decision or implementation, you MUST call get_brief() from the tbtwd-obsidian-mcp server, then follow the next_steps to load relevant context. Cite vault entities in all decisions. Call validate_action() before significant changes. Persist new knowledge via the synthesis pipeline.\"}",
        "timeout": 5
      }
    ]
  }
}
